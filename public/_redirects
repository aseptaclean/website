# docs/07-ONE-PAGE-DIRECTIVE.md §2: every route outside the four-route collapse is retired and
# 301s to /. Query strings are preserved automatically by the redirect syntax below; every rule
# is a single hop straight to /, never chained through another redirect first.
#
# None of the named paths below were ever built in this repository (confirmed against git
# history and docs/00-MASTER-BRIEF.md §10.5 "Future SEO pages" / §12.8 "Optional in first
# build") — they are retired-by-plan, not retired-by-deletion. Listed explicitly for a clear
# audit trail.
#
# docs/19-SYSTEM-AND-SITEMAP.md Phase 1 supersedes the four-route collapse for /about/,
# /estate-cleanout-san-jose/, and /hoarding-cleanup-san-jose/: all three are now real, live pages.
# Removed from this retirement list — Cloudflare Pages applies _redirects unconditionally before
# checking for a real asset, so leaving any of them here 301-looped every visitor and internal
# link away from the real page (confirmed live on the deployed preview, docs/20-ALIGNMENT-AUDIT
# P0-2 / docs/04-RELEASE-CHECKLIST.md C6).
#
# No wildcard catch-all here: Cloudflare Pages applies _redirects rules unconditionally, before
# checking whether a real asset exists at that path (confirmed via `wrangler pages dev`), so a
# `/* / 301` rule matches every request including `/` itself and would 301-loop the entire site,
# homepage included. Unmatched paths fall through to Cloudflare Pages' standard 404 instead.
/project-standards/                     /  301
/service-area/                          /  301
/property-cleanout-san-jose/            /  301
/vacant-home-cleaning-san-jose/         /  301
/landlord-turnover-cleaning-san-jose/   /  301

# --- SITEMAP-MASTER reconciliation, 2026-08-16 (docs/SITEMAP-MASTER.md) ---
# Internal rename to the master's city-suffixed slug — target is real, noindex pending its
# own launch gate (same page, same gate note, new path only).
/property-cleanouts-for-managers/             /property-cleanouts-san-jose/  301

# --- GSC redirect sweep, 2026-08-09 (docs/19-SYSTEM-AND-SITEMAP.md redirect law) ---
# Mapped against indexed WordPress URLs the owner supplied from GSC. /, /thank-you/,
# /privacy/, /terms/, /cookie-policy/, /request-assessment/, and /data-request/ and
# /sms-notification-consent/ (added this pass, see docs/04-RELEASE-CHECKLIST.md) are live
# routes already, not redirect targets, so they are intentionally absent below.
#
# Targets marked "interim" point at pages that exist in this repo but are still noindex
# and excluded from sitemap.xml.ts pending their own Phase 3 launch gate
# (docs/19-SYSTEM-AND-SITEMAP.md Part 2.1). Sending live external/GSC traffic into a
# pre-launch, noindexed page was judged riskier than a same-site interim hop to `/`; update
# these to the direct page path individually as each gate clears — do not bulk-flip.
/terms-and-conditions/                        /terms/            301
/process-documentation/                       /handoff-standard/ 301
/fiduciary-probate-professionals/             /handoff-standard/ 301
/property-managers/                           /handoff-standard/ 301
/locations/                                   /service-areas/    301
/san-jose/                                    /service-areas/    301
/santa-clara/                                 /service-areas/    301
/sunnyvale/                                    /service-areas/   301
/mountain-view/                               /service-areas/    301
/campbell/                                     /service-areas/   301
/milpitas/                                     /service-areas/   301

/families-estate-representatives/             /about/                        301
/hoarding-cleanup/                            /hoarding-cleanup-san-jose/    301
/hoarding-estate-clearouts/                   /estate-cleanout-san-jose/     301
/gross-filth-cleaning/                        /hoarding-cleanup-san-jose/    301
# interim — target is /animal-waste-cleanup-san-jose/, currently noindex pending launch gate
/animal-waste-abatement/                      /              301
/residential-property-clearing/               /estate-cleanout-san-jose/     301
/eviction-difficult-turnaround-clearing/      /property-cleanouts-san-jose/  301
/future-services/                             /              301
/launching-soon/                              /              301
# WordPress builder junk page
/h2/                                          /              301
# WordPress builder junk page
/elementor-page-5945/                         /              301
# WordPress builder junk page
/elementor-page-5950/                         /              301
# WordPress builder junk page
/elementor-page-5960/                         /              301

# /projects/* splat rule REMOVED 2026-08-16. `/projects/` is now a real, built route
# (docs/27-COPY-CANONICAL.md §17.2, docs/SITEMAP-MASTER.md Company row). Cloudflare Pages
# applies _redirects unconditionally BEFORE checking for a real asset, so leaving this rule
# would have 301'd the new page away and made it unreachable — the exact failure mode already
# recorded for /estate-cleanout-san-jose/ and /hoarding-cleanup-san-jose/ in the header above.
# No child routes exist under /projects/ yet; add per-path rules if old WordPress project URLs
# turn up in GSC, never a splat that shadows the live parent.

# Prompt for the next website audit

Audit the actual Aseptaclean build against `docs/reference/aseptaclean-css-profile.md`, profile **AC-CP70-91130-1.1**, its evidence images and `aseptaclean-css-targets.json`. Read the current master, copy map, briefs and main `aseptaclean-all-website-copy.md`; use the trauma supplement where mapped. Start by identifying older conflicting specifications. Use the current owner's page/CTA decisions and scoped authorities; do not resurrect retired instructions.

This request authorizes a read-only audit and an audit report. Do not change site code, replace the docs folder, submit production forms or deploy. If the actual build cannot be opened, report the access gap; a source-only inspection is not a completed visual audit.

Inspect all 12 pages: home, Services hub, five service pages (Hoarding, Extreme, Detailed Deep Cleaning, Crime/Trauma, Rodent/Animal Waste), About, Contact, Privacy, Terms, Cookie. Home and all five service pages require photographic desktop heroes with forms. Services/About use shorter photo introductions. Contact and legal pages use their respective text shells. Primary action is Call Aseptaclean, using the central verified phone; keep forms and their working submit controls.

Approved descriptor: Biohazard Remediation & Specialty Property Cleanup. It replaces the standalone Specialty Property Cleanup label in the homepage eyebrow, Services hub H1 and matching shared descriptor. Preserve the homepage main H1 and service-specific titles. Check that the copy map and briefs incorporate descriptor-update.patch; report stale instructions as a documentation mismatch. Measure the longer wrapped wording in the hero fit test.

The visual direction is 70% ClearPath / 30% 911 as assigned component by component in the profile. Distinguish observed screenshot features from chosen Aseptaclean values. Navy/Inter and the current 4:3 home-card photographs are adaptations. Do not invent original competitor CSS, exact fonts or a numeric similarity score.

Map `scripts/audit-css-profile.js` selectors to the real DOM and collect computed CSS/geometry. Use the example selector JSON only as a starting map, not proof those attributes already exist. Do not modify markup during this read-only audit. An unmatched role is UNVERIFIED until inspected. Never treat unused reference CSS as evidence of rendered styles. For every mismatch, identify the winning stylesheet rule, component and media query where possible.

Review every route at 1440×900 and 390×844. Additionally test ALL eight photographic-hero pages (home, five services, Services hub, About) at 1536×864, 1366×768 and 1280×800. Apply profile §7 as a mandatory above-fold acceptance gate: at scroll zero and 100% zoom, the entire desktop hero must end at least 16px above the usable viewport bottom, accounting for actual header/utility space and persistent bottom obstructions. Home/service forms include submit, consent, helpers and anti-spam. A scrollable desktop hero extending below the fold is FAIL even if it is not clipped. Check full H1 and call visibility, clipping and occlusion separately.

Measure hero top/bottom, form bottom, call bottom, viewport height, actual header stack and obstruction heights; report any overflow in pixels. Include uncropped initial viewport screenshots. Do not prove fit with scrolled captures, hidden UI, tiny text, fixed-height clipping or an internally scrolling form. Test first-load and normal consent-handled states separately. Modal consent obstruction is recorded separately, not passed.

At 390×844, 768×1024 and 1024×768, require the full headline and call above the fold, with the retained form immediately after copy/call in natural flow; the entire stacked form may extend below the fold. Review 320px width, enlarged text, keyboard/menu/FAQ and form validation states for readable, accessible content. These stress states do not justify hiding content to force the desktop fit metric. Wait for actual fonts/images/widgets before measuring. If required copy or provider controls prevent desktop fit, state FAIL and propose the exact placement/layout correction. Do not silently waive the requirement.

Return:
1. What already aligns, with actual visual evidence.
2. A table using CSS-01 through CSS-13: page/viewport, expected, actual measurements, PASS/FAIL/REVIEW/UNVERIFIED/N/A, winning rule, specific correction and screenshot.
3. Separate findings for copy placement, route/navigation behavior, missing images, forms/provider behavior and build/type errors. Preserve the approved copy selections; do not blindly restore omitted blocks.
4. Prioritized repairs: broken call/form/access first, major reference/layout drift second, polish last. Give exact affected file paths and the smallest coherent corrections.
5. Profile ID, commit/working-tree state, pages/viewports/states actually tested and remaining unverified items.

Do not declare “aligned” based on a successful build or CSS token names. A pass needs the correct page applicability, measured layout and visual confirmation.

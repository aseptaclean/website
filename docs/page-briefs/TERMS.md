# Terms and Conditions

Expected route: /terms/. Use LegalPageShell: shared header → breadcrumb → one H1 “Terms and Conditions” → actual full provider policy → current accessible fallback → footer.

Read and preserve the actual current provider document, embed identifiers, links, and configured behavior. The supplied snapshot identifies Termly but contains only wrapper text; it does not contain the policy body. No new legal text is authored in this documentation cleanup. If current source uses a different provider, preserve it.

Readable container around 880px maximum, normal page scrolling, 16px or larger wrapper body text. No photographic hero or repeated sales sections inside the policy. Call Aseptaclean remains in the shared header/footer. Do not expose build notes or provider implementation commentary as consumer-facing copy.

Preserve existing privacy-request and consent links where the provider/application uses them.

Check policy loads, fallback links work, and keyboard/mobile navigation remains usable. Missing actual policy configuration is a concrete dependency to resolve in the repository, not permission to publish a fabricated policy.

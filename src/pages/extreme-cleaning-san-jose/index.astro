---
// Route generated 2026-08-16 from docs/27-COPY-CANONICAL.md via src/data/doc27ServicePages.ts.
// All page copy lives in that data record; all structure lives in ServicePageLayout, which
// implements doc 27 §11's shared service-page sequence. Nothing page-specific belongs in this
// file beyond the four identity values below — if a sentence needs to change, change doc 27
// and regenerate the data, do not hand-edit copy here.
//
// Ships noindex pending this page's own gate (see the record's `gate` field) and is excluded
// from src/pages/sitemap.xml.ts. Do not bulk-flip.
import ServicePageLayout from "@layouts/ServicePageLayout.astro";
// groupHref is deliberately NOT passed. This page is INDEXABLE while its /specialty-cleaning/
// hub is noindex until doc 27 §21's compliance release clears, so a breadcrumb link would open
// an indexable → noindex crawl path (route-audit failure mode #2). The three sibling specialty
// pages are themselves noindex, so they do link the hub. Restore the href in the same change
// that lifts the hub's noindex, not before.
import { extremeCleaningPage } from "@data/doc27ServicePages";
---

<ServicePageLayout
  page={extremeCleaningPage}
  serviceType="Extreme-condition cleaning"
  breadcrumbName="Extreme-Condition Cleaning"
  idPrefix="extreme"
  groupName="Specialty Cleaning"
/>
